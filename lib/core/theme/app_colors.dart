import 'package:flutter/material.dart';

class AppColors {
  // Main background (Sky Blue)
  static const Color skyBlueBg = Color(
    0xFFEFF8FF,
  ); // Very light blue for background
  static const Color lightBlue = Color(
    0xFF70C3EF,
  ); // The blue used in text fields
  static const Color primaryBlue = Color(0xFF1976D2);

  // Distinct Buttons
  static const Color orangeButton = Color(0xFFFF8800);
  static const Color redButton = Color(0xFFFF3D00);
  static const Color darkButton = Color(0xFF263238);

  // Neutral Colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color greyText = Color(0xFF757575);
  static const Color greyBg = Color(0xFFF5F5F5);

  // Success/Error
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
}
